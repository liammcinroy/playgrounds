from setuptools import setup


setup(name='trex',
      version='0.1',
      packages=[],
      package_dir={},
      install_requires=['flake8',
                        'pytest',
                        'triton',
                        'torch'],
      python_requires='>=3.0')
